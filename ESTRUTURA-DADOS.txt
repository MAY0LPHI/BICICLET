╔════════════════════════════════════════════════════════════════════╗
║           ESTRUTURA PADRÃO DE DADOS - WEB E DESKTOP                ║
║                                                                    ║
║  Todos os dados são organizados da mesma forma em ambas as versões ║
╚════════════════════════════════════════════════════════════════════╝


📁 ESTRUTURA FINAL
═══════════════════

dados/
├── navegador/           ← Versão WEB (Browser)
│   ├── clientes/        ← Cada cliente em um arquivo JSON (por CPF)
│   │   ├── 12345678900.json
│   │   └── 98765432100.json
│   └── registros/       ← Cada registro em um arquivo JSON (por ID)
│       ├── reg-001.json
│       └── reg-002.json
│
└── desktop/             ← Versão DESKTOP (Electron)
    ├── clientes/        ← Cada cliente em um arquivo JSON (por CPF)
    │   ├── 12345678900.json
    │   └── 98765432100.json
    ├── registros/       ← Cada registro em um arquivo JSON (por ID)
    │   ├── reg-001.json
    │   └── reg-002.json
    ├── usuarios.json    ← Lista de usuários (na pasta raiz)
    ├── auditoria.json   ← Log de auditoria (na pasta raiz)
    └── categorias.json  ← Categorias (na pasta raiz)


📋 EXPLICAÇÃO
═══════════════

CLIENTES:
  • Cada cliente é salvo em um arquivo separado
  • Nome do arquivo = CPF sem pontos/hífens (ex: 12345678900.json)
  • Facilita busca rápida e controle de versão

REGISTROS:
  • Cada registro é salvo em um arquivo separado
  • Nome do arquivo = ID único do registro
  • Facilita auditoria e rastreamento

CONTROLE (Desktop):
  • usuarios.json    → Lista de usuários do sistema
  • auditoria.json   → Log detalhado de ações
  • categorias.json  → Categorias de bicicletas


✅ VANTAGENS DESSA ESTRUTURA
═══════════════════════════════

1️⃣  PADRONIZAÇÃO
   → Web e Desktop usam o mesmo padrão
   → Fácil migração de dados entre versões

2️⃣  PERFORMANCE
   → Arquivos menores = carregamento mais rápido
   → Busca otimizada por cliente/registro

3️⃣  CONTROLE DE VERSÃO
   → Cada cliente/registro é independente
   → Melhor rastreamento de mudanças

4️⃣  ESCALABILIDADE
   → Fácil adicionar novos campos
   → Estrutura preparada para crescimento


🔄 MIGRAÇÃO AUTOMÁTICA
═══════════════════════

Ao iniciar a aplicação:
  ✓ Se arquivo antigo existir → Convertido automaticamente
  ✓ Nova estrutura criada
  ✓ Dados preservados


⚠️  IMPORTANTE
════════════════

• Não edite os arquivos .json manualmente (pode quebrar dados)
• Use a aplicação para adicionar/editar clientes e registros
• Sempre faça backup da pasta "dados" antes de atualizações


═══════════════════════════════════════════════════════════════════

Estrutura: Versão 2.3.0 - Padronizada
